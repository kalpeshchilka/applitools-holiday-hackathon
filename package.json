{
  "name": "applitools-holiday-hackathon",
  "version": "1.0.0",
  "description": "This project is entry for Applitools Holiday Shopping Hackathon",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "test:v1": "cypress run --config-file cypress.v1.json --browser chrome",
    "test:dev": "cypress run --config-file cypress.dev.json --browser chrome",
    "test:v2": "cypress run --config-file cypress.v2.json --browser chrome"
  },
  "author": "kalpesh",
  "license": "ISC",
  "devDependencies": {
    "@applitools/eyes-cypress": "^3.15.5",
    "cypress": "^5.6.0"
  }
}
